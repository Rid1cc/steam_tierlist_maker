import { Game } from '@/types/game'

// Empty by default to prevent 404 spam from non-existent Steam images
// Users should import their real Steam library using "Import Steam Games" button
export const steamGames: Game[] = []